int data[];
int interval;
int i;
int n;
scan("%d", n);
for (i = 0, i<n, i++);
{;
  scan("%d", data[i]); 	
};
for (interval = n / 2, interval > 0, interval = interval/2); 
{;
  for (i = interval, i < n, i++); 
  {;
    int temp = data[i];
    int j;
    for (j = i, j >= interval & data[j - interval] > temp, j = j - interval); 
    {;
     	data[j] = data[j - interval];
    };
    data[j] = temp;
  };
};
for (i = 0, i < size, i++) ;
{;
  printf("%d  ", data[i]);
};
